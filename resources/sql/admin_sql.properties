selectMemberAll=SELECT * FROM (SELECT ROWNUM AS RNUM, A.* FROM (SELECT * FROM MEMBER ORDER BY ENROLL_DATE DESC) A ) WHERE RNUM BETWEEN ? AND ?
selectMemberCount=SELECT COUNT(*) FROM MEMBER
selectMemberDetail=SELECT * FROM MEMBER WHERE MEMBER_NUM=?
deleteMember=DELETE FROM MEMBER WHERE MEMBER_NUM=?
updateMember=UPDATE MEMBER SET MEM_DELETE=? WHERE MEMBER_NUM=?
searchMemberCount=SELECT COUNT(*) FROM MEMBER WHERE @ LIKE ?
searchMember=SELECT * FROM (SELECT ROWNUM AS RNUM, A.* FROM (SELECT * FROM MEMBER WHERE @ like ?)A) WHERE RNUM BETWEEN ? AND ?

selectBoardList=SELECT * FROM (SELECT ROWNUM AS RNUM, A.* FROM (SELECT * FROM BOARD WHERE B_DELETE=? ORDER BY B_DATE DESC)A) WHERE (RNUM BETWEEN ? AND ?)
selectAliveBoardCount=SELECT COUNT(*) FROM BOARD WHERE B_DELETE=?

searchBoard=SELECT * FROM (SELECT ROWNUM AS RNUM, A.* FROM (SELECT * FROM BOARD WHERE @ like ? AND B_DELETE=0)A) WHERE RNUM BETWEEN ? AND ?
searchBoardCount=SELECT COUNT(*) FROM BOARD WHERE @ LIKE ? AND B_DELETE=0

SellBoardList=SELECT * FROM (SELECT ROWNUM AS RNUM, A.* FROM (SELECT * FROM BOARD WHERE B_SELL=? ORDER BY B_DATE DESC)A) WHERE (RNUM BETWEEN ? AND ?)
SellBoardListCount=SELECT COUNT(*) FROM BOARD WHERE B_SEll=1

SoldBoardList=SELECT * FROM (SELECT ROWNUM AS RNUM, A.* FROM (SELECT * FROM BOARD WHERE B_SELL=? ORDER BY B_DATE DESC)A) WHERE (RNUM BETWEEN ? AND ?)
SoldBoardListCount=SELECT COUNT(*) FROM BOARD WHERE B_SEll=0